package com.alsselssajob.mattermostapi.common.vo.qualifier;

public enum PostQualifier {
    channel_id,
    root_id,
    parent_id,
    created_at,
    created_date,
    message,
    is_scrapped,
    hashTag
}
